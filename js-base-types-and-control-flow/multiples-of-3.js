// In a for loop, we do not simply a condition
// A for loop has:
// - variable initialization
// - condition
// - increment (code that executes after)

// for (<variable initialization>; <condition>; <increment code>) { ....
for (let i = 0; i <= 100; i += 3) {
  console.log(i);
}
